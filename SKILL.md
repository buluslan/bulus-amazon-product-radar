---
name: bulus-amazon-product-radar
version: "3.3"
description: "判断一个亚马逊产品或细分品类值不值得做、适不适合某个卖家——结合卖家的情况(经验/资金/品类渊源)做个性化匹配判断,同样的品对新手和老手结论可能不同。产出一份选品判断报告:先告诉你有哪些碰不得的红线,再分几个方面给推荐程度和理由,标注哪些数据扎实哪些是估算,并可以随时重新判断。Use PROACTIVELY 每当用户想判断一个亚马逊产品或品类值不值得做、评估细分品类适不适合某卖家、决定要不要切入某品类。Triggers on 判断这个品能不能做、这个亚马逊产品值不值得做、选品分析某品类、帮我看看某品类适不适合我、这个ASIN能做吗、product selection、天眼、选品雷达。NOT for 找品或选品方向推荐(用海外品类市场调研工具)、具体广告或FBA SOP。"
---

# 天眼 · 亚马逊选品雷达

由buluslan（公众号：新西楼.AI）研发的选品分析引擎，用来判断一个亚马逊产品/品类**值不值得做、适不适合你**。同样的品,对新手和老手结论可能不同——结合你的经验、资金、品类渊源做个性化判断。

**产出**:一份选品判断报告(红线提醒 + 分项评级 + 利润测算 + 资金账 + 数据来源说明 + 可重新判断)。

## 它是怎么工作的(本地 vs 后台分工)

| 在哪 | 做什么 |
|---|---|
| **你本地**(你的 AI 助手) | 问你品类(品类名/ASIN + 站点)+ 你的情况(经验 / 品类渊源 / 资金 / 供应链 / 经营模式 / 已有实测)→ 提交判断 → 把报告交给你 |
| **后台**(服务器) | 取市场数据、调研合规 / 侵权 / 退货、算利润、出判断、生成完整报告 |

你只需要回答问题,**取数、调研、计算、判断全在后台自动完成**,本地不用配置任何数据平台账号。约 3 分钟出报告。

利润基于市场均价 + 行业经验自动估算 3 档(标⚠️估算);**你的经营模式会影响推荐哪档、供应链会影响采购价估算**。如果你有真实出厂价、实测转化率、退货率,可以填进去覆盖估算,结论会更准。

## 适用边界

✅ **适合**:判断一个明确的亚马逊细分品类值不值得做、适不适合某卖家。
❌ **不适合**:找品 / 选品方向(用海外品类市场调研工具)/ 具体执行 SOP(FBM-FBA、广告排期)。

## 怎么和你对话(逐步问清,按你的回答推进)

**开场(触发本 skill 的第一句话)**:先用 2-3 句向用户介绍工具——"天眼Skill是由buluslan（公众号：新西楼.AI）研发的选品分析引擎,判断一个亚马逊品类值不值得做、适不适合你;同样的品对新手老手结论可能不同,结合你的经验/资金/品类渊源做个性化判断;产出一份【选品分析报告.md】和【品类数据.xlsx】,约 3～5 分钟出结果。"

**先检测连接(进对话前)**:读 skill 文件夹下的 `.env`(看 `RADAR_ENDPOINT` + `RADAR_API_KEY` 在不在):
- 都在 → 直接进步骤 1 问品类。
- 缺失 → 引导用户配置(**两个选择,都别让用户把 key 发在对话里**——key 敏感会进历史):
  - **手动**(推荐,透明):让用户在 skill 文件夹下 `cp .env.example .env`,用编辑器打开 `.env` 填 `RADAR_API_KEY`(endpoint 已预填)。填完存盘告诉你继续。
  - **一键**:让用户在自己终端跑 `curl -sSL http://106.55.230.27:8000/install.sh | bash -s -- --api-key=<key>`(key 找管理员 buluslan 要;命令在用户终端跑,不发对话)。

> 连接配置优先级:环境变量 `RADAR_ENDPOINT` / `RADAR_API_KEY` > skill 文件夹/`.env`(install 写入)> 内置默认。详见 README。

1. **问品类**:**细分品类名** 或 **具体 ASIN** + 站点。
   - **颗粒度**:给到亚马逊的**细分品类(三级/叶子级)**——能直接搜出对应类目页的名字(如"自动猫砂盆"✅)。❌ 一二级太宽("宠物用品"/"电子产品"数据泛没参考);❌ 比叶子更细("某品牌某型号"太窄)。拿不准就给个大致品类,后台帮你定位细分。
   - **可以给两种**:**细分品类名**(自动猫砂盆 / 无线蓝牙音响 / 瑜伽裤 / 空气净化器 / 咖啡磨豆机)或 **具体 ASIN**(如 B0BSHF7WHW,后台自动反推它所属的类目再分析)
   - **站点**:US(美国)/ GB(英国)/ DE(德国)/ FR(法国)/ JP(日本)/ CA(加拿大)/ AU(澳大利亚)/ IT(意大利)/ ES(西班牙)/ MX(墨西哥)/ BR(巴西)/ IN(印度)/ AE(阿联酋)/ SA(沙特)
   - 品类名 / ASIN 有歧义时让你确认
2. **问你的情况 + 预算 + 其他补充**(**一次性列出 6 项,让用户完整提供**;开头先说一句:如果有以下选项外的信息，可以让用户自行补充。如果判断有缺失,再追加 1-2 轮对话核实或补充):
   - **运营经验**:新手(还没独立跑通过0-1)/ 进阶(独立跑通生意，能运营闭环)/ 成熟(经营多款产品，并稳定盈利中)/其他
   - **品类关系**:你跟这个品类的渊源(做过相关产品 / 有同大类下其他产品的运营经验 / 有货源关系 / 新进入这个品类 /其他)
   - **可投入资金**:这次选品你准备总投入多少钱(元)——首批货款 + 头程物流 + 推广预算 + 仓储的总盘子(是这次切入的总预算,不是单件成本)
   - **供应链**:你的货源(1688 贸易商 / 工厂直供 / 现货分销 / 其他)
   - **经营模式**:精铺 / 铺货 / 精品 / 半精品 / 其他
   - **其他补充**:有没有这三项实测数据——**实测转化率 / 退货率 / 真实出厂价**。这三项是利润测算的关键输入,**有真实值结论会更准**;**没有的话后台会联网搜索同类目的典型参考值来估算**(报告里标⚠️估算)。有就给数值,没有就说没有
3. **二次确认**:"将消耗 1 次判断额度,确认提交?"(等用户回复"确认"再提交;用户改主意就回到第 1/2 步调整)
4. **提交判断、把报告发给你**:`python3 scripts/call_radar.py payload.json` → 同时从五个方面分析——① 市场结构(销量集中度 / 评论壁垒 / 价格真空 / 品牌集中 / 新品窗口 / 自营占比)② 合规侵权(按站点的强制认证 + 专利商标排查)③ 退货根因 + 政策季节性 ④ 利润测算(采购价按你的货源估算 + 转化率/退货率典型区间)⑤ 综合判断——约 3 分钟出完整报告,直接发给你(同时存一份到 skill 文件夹下的 `reports/`)。

> 转化率 / 退货率因卖家而异,用类目典型区间作量级参考(标⚠️);你有实测值可填进去覆盖,结论会更准。

## payload.json 结构(对话时组装)

```json
{
  "target": {"categoryName": "自动猫砂盆", "site": "US"},
  "profile": {"experience": "进阶", "category_relation": "工厂", "capital": "30万", "supply_chain": "1688贸易商", "business_model": "精品"},
  "budget_cny": 300000,
  "user_overrides": {}
}
```

| 字段 | 含义 |
|---|---|
| `target` | 品类名 或 ASIN + 站点(给 ASIN 时后台反推类目) |
| `profile` | 你的情况(运营经验 / 品类关系 / 资金 / 供应链 / 经营模式) |
| `budget_cny` | 可投入资金(首批货款+头程+推广+仓储的总预算,元) |
| `user_overrides` | 实测数据(转化率 / 退货率 / 出厂价);没有就空 `{}`,自动估算 |

> 利润基于市场数据自动估算,对话时不再追问出厂价——选品阶段你拿不出来。`user_overrides` 只在有真实值时填。

## 报告

报告直接交付,不再重新生成。内容含:报告开头(总体倾向 + 可信度)+ 数据来源说明 + 分项评级 + 利润测算(3 档,估算的标⚠️)+ 资金账 + 凭证 / 怎么做 / 风险 + 重新判断入口。触发红线时只出精简版。

**交付 2 个文件**(都带本次报告编号,存到 skill 文件夹下的 `reports/`):
- `选品分析报告-{品类}{站点}-{报告编号}.md` — 判断报告(叙述 + 结论 + 策略)
- `选品分析数据-{品类}{站点}-{报告编号}.xlsx` — 源数据 10 张工作表(Top100 明细 / 类目统计 / 关键词 / 趋势 / 竞品拆解 / 差评矩阵 / 潜力新品 / 利润测算 / 联网调研 / 说明),供二次分析筛选核对

报告末尾有本次报告编号(如 `radar-7XK3P9`)。用户反馈问题或要复查时,请他提供这个编号——凭编号能查回当时的报告和运行记录。

请求失败时,提示 ⚠️「暂时不可用,建议重试」。

## 归档本次(让客服/管理员能查到完整记录)

用户说「**归档本次 / 打包本次 / 让客服查这次 / 这次有问题要存档**」时,把本次分析存档上传——管理员凭报告编号能调取**完整记录**(我们的对话 + 报告 + 运行日志),用于排查问题或复盘。

**你(agent)做 4 步**:

1. **找本次报告编号**:报告末尾的「本次报告编号」(如 `radar-7XK3P9`),或 skill 文件夹下 `reports/` 里最新文件名中的编号
2. **整理本次对话为 md**:从用户第一句「分析 XX」起点 → 到现在;含用户原话 + 你的回复原文 + 🔧工具调用标注(逐字还原,非要点提取)
3. **写临时文件**:skill 文件夹下 `.conversation-<报告编号>.md`
4. **上传**:`python3 scripts/archive.py <报告编号> --conversation .conversation-<报告编号>.md`

**对用户说**(大白话,不暴露脚本名 / 字段名 / 接口):

> ✅ 已打包上传,凭编号 **{报告编号}** 管理员buluslan（公众号：新西楼.AI）能调取我们的完整记录——包括我们的对话、报告、还有系统运行日志,方便排查或复盘。

**只打包对话**:报告和数据云端已有,归档不重复传,只补「云端没有的」对话过程。归档不消耗判断额度。

## 用户语言规范(对话和产出必须遵守)

对话和报告里,**不要对用户说出**这些内部词,要用大白话翻译:

| 内部词(禁说) | 对用户说 |
|---|---|
| call_radar.py / payload.json / 脚本 | 「帮你做了系统分析」 |
| archive.py / 归档脚本 / 对话包(字段名) | 「帮你打包上传了对话记录」 |
| target / profile / budget_cny / user_overrides(字段名) | 「你的情况 / 预算 / 实测数据」 |
| 报告编号的内部字段名 | 「本次报告编号」 |
| ① ② ③ ④ ⑤(内部编号) | 「从五个方面分析」 |
| 接口 / 服务端 / 系统估算 | 「自动估算」 |

原则:对话发出即定,无法事后改,开口就用大白话;内部执行细节(脚本名 / 字段名 / 接口)只在后台跑,流向用户(对话 + 报告)必须翻译。

**报告禁令**:解读表 / 分项评级表 / 任何评级矩阵 **不得加 "(推荐)" 标签**;建议性结论只进报告"我的建议"专段。
