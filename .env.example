# 天眼选品雷达 · 连接配置
# 用法:复制本文件为 .env,然后填 RADAR_API_KEY(endpoint 已预填默认)
#   cp .env.example .env
#   # 用编辑器打开 .env,在下面 RADAR_API_KEY= 后填你的 key(找管理员 buluslan 要)
# .env 不进 git(已 gitignore),只在你本地;填完即可用。

RADAR_ENDPOINT=http://106.55.230.27:8000
RADAR_API_KEY=
